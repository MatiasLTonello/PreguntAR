<script defer>
    document.title = `Respuesta incorrecta`;
</script>
<div class="container mx-auto px-4 min-h-screen flex flex-col items-center pt-8">
    <!-- Mensaje de respuesta incorrecta -->
    <div class="alert alert-error shadow-lg max-w-2xl w-full mb-6">
        <div>
            <svg xmlns="http://www.w3.org/2000/svg" class="stroke-current flex-shrink-0 h-6 w-6" fill="none" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 14l2-2m0 0l2-2m-2 2l-2-2m2 2l2 2m7-2a9 9 0 11-18 0 9 9 0 0118 0z" />
            </svg>
            <div>
                <h1 class="text-4xl font-bold">¡Respuesta Incorrecta!</h1>
                <p class="text-lg">La respuesta correcta era: <strong>{{opcionCorrecta}}</strong></p>
            </div>
        </div>
    </div>

    <!-- Mostrar puntaje final -->
    <div class="alert alert-info shadow-lg max-w-2xl w-full mb-8">
        <div>
            <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" class="stroke-current flex-shrink-0 w-6 h-6">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            <h2 class="text-2xl font-bold">Puntaje Final: <strong>{{puntaje}}</strong></h2>
        </div>
    </div>

    <!-- Botones de acción -->
    <div class="flex flex-wrap justify-center gap-4">
        <a href="/" class="btn btn-neutral">Volver al Menú Principal</a>
        <button class="btn btn-warning" onclick="window.my_modal_1.showModal()">Reportar Pregunta</button>
        <a href="/partida" class="btn btn-primary">Iniciar Nueva Partida</a>
    </div>

    <!-- Modal -->
    <dialog id="my_modal_1" class="modal">
        <form method="post" action="/report/reportarPregunta" class="modal-box bg-gray-100 text-black p-6 rounded-lg shadow-lg">
            <h3 class="font-bold text-lg mb-3 text-gray-800">Reportar pregunta</h3>
            <div class="py-4">
                <input type="hidden" id="idPregunta" name="idPregunta" value="1" />
                <input type="text" id="motivo" name="motivo" placeholder="Motivo del reporte" class="input input-bordered w-full text-white" />
            </div>
            <div class="modal-action flex justify-end gap-3">
                <button type="button" class="btn btn-outline btn-neutral" onclick="document.getElementById('my_modal_1').close()">Cancelar</button>
                <button type="submit" class="btn btn-primary">Enviar</button>
            </div>
        </form>
    </dialog>

</div>
<script>
    // Obtener el parámetro de la URL
    const urlParams = new URLSearchParams(window.location.search);
    const idPregunta = urlParams.get('pregunta');

    // Asignar el valor al input
    if (idPregunta) {
        document.getElementById('idPregunta').value = idPregunta;
    }
</script>