<script defer>
    document.title = `Respuesta incorrecta`;
</script>
<main class="container text-center my-5 flex-fill">
    <!-- Mensaje de respuesta incorrecta -->
    <div class="alert alert-danger" role="alert">
        <h1 class="display-4">¡Respuesta Incorrecta!</h1>
        <p class="fs-5">La respuesta correcta era: <strong>{{opcionCorrecta}}</strong></p>
    </div>

    <!-- Mostrar puntaje final -->
    <div class="alert alert-info mt-3" role="alert">
        <h2 class="display-6">Puntaje Final: <strong>{{puntaje}}</strong></h2>
    </div>

    <!-- Botones de acción -->
    <div class="d-flex justify-content-center gap-3 mt-4">
        <a href="/" class="btn btn-secondary btn-lg">Volver al Menú Principal</a>
        <button class="btn btn-warning btn-lg" data-bs-toggle="modal" data-bs-target="#reportarModal">Reportar Pregunta</button>
        <a href="/partida" class="btn btn-primary btn-lg">Iniciar Nueva Partida</a>
    </div>

    <!-- Modal -->
    <aside class="modal fade" id="reportarModal" tabindex="-1" aria-labelledby="reportarModalLabel" aria-hidden="true">
      <div class="modal-dialog">
        <form class="modal-content" action="/report/reportarPregunta" method="post">
          <div class="modal-header">
            <h1 class="modal-title fs-5" id="reportarModalLabel">Reportar pregunta</h1>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Cerrar"></button>
          </div>
          <div class="modal-body">
            <input type="hidden" id="idPregunta" name="idPregunta" value="14" />
            <input class="form-control" type="text" id="motivo" name="motivo" placeholder="Motivo del reporte" />
          </div>
          <div class="modal-footer">
            <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
            <button type="submit" class="btn btn-primary">Enviar</button>
          </div>
        </form>
      </div>
    </aside>

</main>
